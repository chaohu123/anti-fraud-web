# 项目功能详细介绍文档（反诈信息识别与风险自测平台）

> 文档用途：用于毕业设计论文撰写、答辩展示与项目交付说明。  
> 项目形态：前后端分离 Web 系统（用户端 + 管理端）。  
> 后端服务默认端口：8081；前端开发服务默认端口：5173（Vite 代理 `/api`、`/uploads` 到后端）。  

---

## 1. 项目概述

本项目面向普通用户（以高校学生为主要对象），提供“诈骗案例识别训练 + 防骗知识学习 + 风险测评与报告 + 成就激励 + 后台运营管理”的一体化平台。系统目标是通过可交互的训练与可解释的测评反馈，帮助用户提升反诈识别能力，并通过后台管理实现内容持续维护与数据统计分析，形成可持续迭代的反诈教育闭环。

---

## 2. 用户角色与权限边界

### 2.1 普通用户（User）

- **主要能力**：注册登录、训练、测评、学习、防骗档案查看、成就查看。
- **访问范围**：前台全部页面；不可访问 `/admin/**` 管理端页面。

### 2.2 管理员（Admin）

- **主要能力**：内容维护（案例/题库/知识/轮播/成就）、用户数据查看、统计分析、系统设置。
- **访问范围**：在前端通过路由守卫进行简化控制：`username === 'admin'` 才可进入管理端路由（见 `frontend/src/router/index.ts`）。
- **说明**：后端管理接口目前以毕设展示为主，鉴权未实现完整 RBAC/JWT 方案，若用于真实部署环境，需补齐统一认证授权与接口级权限控制。

---

## 3. 用户端功能说明（前台）

本节以“页面—功能点—后端支撑接口（如有）—数据落库/缓存”的方式进行说明，便于论文和答辩按模块展开。

### 3.1 登录与注册（`/login`）

- **功能点**
  - 用户注册：创建账号并完善基础信息（用户名唯一校验）。
  - 用户登录：校验账号密码，获取 `userId/username` 建立前端会话。
- **后端接口**
  - `POST /api/users/register`：注册
  - `POST /api/users/login`：登录
- **关键实现说明**
  - 密码采用 BCrypt 加密存储（安全性基础要求）。
  - 前端会话使用本地存储 `af_user_session`（仅保存当前身份，避免将业务数据混入会话）。

### 3.2 首页（`/`）

- **功能点**
  - 展示平台入口与功能导览（训练、测评、知识库、个人中心等）。
  - 可结合轮播内容进行防骗提示（若页面接入轮播数据）。
- **后端接口（可选）**
  - 若首页展示轮播：`GET /api/carousel`

### 3.3 诈骗案例识别训练（`/train`）

- **功能点**
  - 随机获取案例并完成“是否诈骗”的判断训练。
  - 支持按类型/难度筛选（视 UI 实现而定）。
  - 训练完成后记录训练结果与正确性，用于统计与成就。
- **后端接口**
  - `GET /api/cases/random`：随机获取案例
  - `GET /api/cases/{id}`：案例详情
  - `GET /api/cases/page`：分页获取（含管理/扩展用途）
  - `POST /api/train/records`：提交训练记录
  - `GET /api/train/stats/{userId}`：训练统计（总次数、正确率、按类型错误率等）
- **数据记录**
  - 表：`af_fraud_case`（案例库）、`af_training_record`（训练记录）
- **业务规则说明**
  - 案例字段包含 `type`（sms/email/audio/site）、`level`（easy/medium/hard）、`answer`（fraud/safe）、`suspicious_tags`（JSON）等，用于构建接近真实情境的训练素材。

### 3.4 风险测评（`/assessment`）与报告详情（`/report/:id`）

- **功能点**
  - 获取问卷题目与选项，完成作答提交。
  - 生成测评报告：维度分数、综合分、风险等级、解释文本与建议列表。
  - 支持查看历史报告详情（前端支持本地历史存储；后端也会落库记录）。
- **后端接口**
  - `GET /api/questions`：获取风险测评题目（含选项）
  - `POST /api/assessment`：提交作答并返回报告
- **数据记录**
  - 题库：`af_risk_question`、`af_risk_option`
  - 结果：`af_risk_assessment`（防骗风险测评结果表）
  - 用户画像字段：`af_user.risk_level`（0低/1中/2高）、`af_user.last_score`
- **核心算法（可解释评分）**
  - 按 INFO/FINANCE/PSYCH 三维度分别计算加权得分并进行归一化到 0–100；
  - 综合分为三维度平均值；
  - 风险等级按阈值分级：\<40 低风险（LOW）、40–70 中风险（MEDIUM）、≥70 高风险（HIGH）；
  - 输出 `explanation`（说明）与 `suggestions`（建议列表），提升报告可理解性与可用性。
- **前端本地缓存（账号隔离）**
  - 测评报告历史：`af_assessment_u_{userId}`（游客为 `af_assessment_guest`），默认最多保留 50 条，用于“快速回溯展示”和“离线/弱网下的演示稳定性”。

### 3.5 防骗知识库（`/knowledge`、`/knowledge/:id`、`/knowledge/learned`）

- **功能点**
  - 按分类与关键词搜索知识文章，查看详情并记录学习进度。
  - 展示已学知识列表与学习完成率，形成学习闭环。
- **后端接口**
  - `GET /api/knowledge`：知识列表（支持 `category`、`q`）
  - `GET /api/knowledge/{id}`：知识详情
  - `POST /api/knowledge/{id}/learn`：上报学习进度（进度 0–100，100 视为完成）
  - `GET /api/knowledge/progress/{userId}`：学习进度统计与已完成 ID 列表
- **数据记录**
  - 表：`af_knowledge`（知识条目）、`af_learning_record`（学习记录）
- **业务规则说明**
  - 知识条目包含 `category`、`difficulty(1-5)`、`enable_flag` 等字段，便于后台运营管理与前端展示筛选。
  - 学习记录采用 `user_id + knowledge_id` 唯一约束，避免重复插入，确保统计口径一致。

### 3.6 个人中心（`/user-center`）

- **功能点**
  - 查看并维护个人信息（昵称/头像等）。
  - 查看个人风险等级、训练统计、学习进度等综合画像（视 UI 组合展示）。
- **后端接口**
  - `GET /api/users/{userId}`：用户信息
  - `PUT /api/users/{userId}/avatar`：更新头像
  - `PUT /api/users/{userId}/info`：更新昵称等
  - `PUT /api/users/{userId}/password`：修改密码
- **数据记录**
  - 表：`af_user`（头像 `avatar_url`、昵称 `nickname`、风险等级等）

### 3.7 成就中心（`/achievement`）

- **功能点**
  - 展示成就列表、完成条件、解锁状态与经验奖励。
  - 通过激励机制提升用户持续训练/学习意愿。
- **实现特点**
  - 支持“后端可配置规则 + 前端回退内置规则”的双通路：当后台可用时从管理接口加载 ACTIVE 成就规则；当接口不可用时仍可使用内置规则保证演示完整性（适合毕设场景）。

### 3.8 实时通知（可选功能）

- **功能点**
  - 通过实时推送向用户展示学习完成、成就解锁、风险变化等通知（具备扩展空间）。
- **后端能力**
  - WebSocket（STOMP + SockJS）：端点 `/ws`；广播 `/topic/notifications`；点对点 `/user/queue/notifications`。

---

## 4. 管理端功能说明（后台）

管理端以“内容运营 + 数据分析”为核心，面向管理员提供可视化管理界面。管理端路由均位于 `/admin/**`，由 `AdminLayout` 统一布局承载。

### 4.1 仪表盘（`/admin/dashboard`）

- **功能点**
  - 展示用户规模与风险分布、近期活跃趋势等运营指标。
  - 使用图表组件（ECharts）进行可视化展示，便于答辩现场演示。
- **后端接口（统计）**
  - `GET /api/admin/statistics/users`：用户总数
  - `GET /api/admin/statistics/risk-distribution`：风险等级分布
  - `GET /api/admin/statistics/active-trend`：近 7 天活跃趋势（以测评记录等口径统计）

### 4.2 用户数据管理（`/admin/users`）

- **功能点**
  - 查看用户列表与关键画像字段（如风险等级）。
  - 支持查看某用户报告/概览数据（用于教学或演示）。
- **后端接口（示例）**
  - `GET /api/admin/users`
  - `GET /api/admin/users/{id}/report`

### 4.3 诈骗案例管理（`/admin/cases`）

- **功能点**
  - 案例新增、编辑、删除、批量删除；启用/停用控制。
  - 维护训练素材库，前台训练模块直接复用该数据。
- **后端接口**
  - `GET /api/admin/cases`
  - `POST /api/admin/cases`
  - `PUT /api/admin/cases/{id}`
  - `DELETE /api/admin/cases/{id}`
  - `DELETE /api/admin/cases/batch`

### 4.4 训练题目管理（`/admin/training`）

- **功能点**
  - 维护训练题目（通常基于案例转换/抽象），支持增删改查与批量删除。
  - 用于组织训练内容展示与难度控制（与前台训练形成联动）。
- **后端接口**
  - `GET /api/admin/training/questions`
  - `POST /api/admin/training/questions`
  - `PUT /api/admin/training/questions/{id}`
  - `DELETE /api/admin/training/questions/{id}`
  - `DELETE /api/admin/training/questions/batch`

### 4.5 风险测评问卷管理（`/admin/assessment`）

- **功能点**
  - 管理测评题目与选项：新增题目、配置维度/权重/题型、维护选项及分值。
  - 调整题库配置后，前台问卷内容与评分结果会随之变化，便于展示“可运营、可配置”的系统特征。
- **后端接口**
  - `GET /api/admin/assessment/questions`
  - `POST /api/admin/assessment/questions`
  - `PUT /api/admin/assessment/questions/{id}`
  - `DELETE /api/admin/assessment/questions/{id}`
  - `DELETE /api/admin/assessment/questions/batch`

### 4.6 知识库管理（`/admin/knowledge`）

- **功能点**
  - 知识文章的新增、编辑、删除、批量删除；分类/标签/难度维护；启用/停用控制。
  - 直接驱动前台知识库展示与学习进度统计。
- **后端接口**
  - `GET /api/admin/knowledge`
  - `POST /api/admin/knowledge`
  - `PUT /api/admin/knowledge/{id}`
  - `DELETE /api/admin/knowledge/{id}`
  - `DELETE /api/admin/knowledge/batch`

### 4.7 轮播图管理（`/admin/carousel`）

- **功能点**
  - 轮播项新增/编辑/删除；上传图片；排序与启用控制。
  - 支持“上传即用”：上传完成后返回 `/uploads/...` 可直接被前端引用，适合答辩现场展示闭环。
- **后端接口**
  - `GET /api/admin/carousel`
  - `POST /api/admin/carousel`
  - `PUT /api/admin/carousel/{id}`
  - `DELETE /api/admin/carousel/{id}`
  - `POST /api/admin/carousel/upload`
  - `POST /api/admin/carousel/upload/batch`
- **前台轮播接口**
  - `GET /api/carousel`

### 4.8 成就规则管理（`/admin/achievement`）

- **功能点**
  - 成就规则增删改查与批量删除；启用/停用控制。
  - 前端成就中心可优先加载后台配置规则，实现“规则可配置化”。
- **后端接口**
  - `GET /api/admin/achievements`
  - `POST /api/admin/achievements`
  - `PUT /api/admin/achievements/{id}`
  - `DELETE /api/admin/achievements/{id}`
  - `DELETE /api/admin/achievements/batch`

### 4.9 系统设置（`/admin/settings`）

- **功能点**
  - 用于配置阈值、经验等系统参数（当前实现以参数校验与返回成功为主，属于扩展点）。
- **后端接口**
  - `GET /api/admin/settings`
  - `PUT /api/admin/settings`

---

## 5. 接口与数据返回约定（前后端交互规范）

### 5.1 REST 接口风格

- 统一以 `/api` 为前缀。
- 管理接口以 `/api/admin` 为前缀。
- 前端通过 Axios 调用相对路径（`baseURL: '/api'`），由 Vite 代理转发到后端服务。

### 5.2 统一返回结构（Result）

后端接口统一返回结构形如：

```json
{
  "success": true,
  "code": 0,
  "message": "ok",
  "data": {}
}
```

前端在 `src/api/http.ts` 中对该结构进行统一处理：当 `success=true` 时解包 `data` 供页面直接使用；当 `success=false` 时抛出错误并统一提示，减少页面重复判断逻辑。

---

## 6. 核心数据模型说明（数据库表）

### 6.1 用户与画像

- `af_user`：用户基础信息与画像字段（风险等级 `risk_level`、最近得分 `last_score`、头像 `avatar_url` 等）。

### 6.2 训练与案例

- `af_fraud_case`：诈骗案例库（类型/难度/标准答案/可疑标签等）。
- `af_training_record`：训练记录（用户判断、是否正确、用时、选择的可疑点等）。

### 6.3 风险测评题库与结果

- `af_risk_question`：测评题目（维度 `dimension`、权重 `weight`、题型 `question_type`）。
- `af_risk_option`：测评选项（分值 `option_value`）。
- `af_risk_assessment`：测评结果（维度分、综合分、风险等级、解释文本与建议列表）。

> 说明：`schema.sql` 同时包含 `af_assessment_result`（旧结构/备用表），当前业务以 `af_risk_assessment` 为主。

### 6.4 知识库与学习记录

- `af_knowledge`：防骗知识条目（分类、摘要、正文、难度、标签、启用状态）。
- `af_learning_record`：学习进度（`progress` 0–100、完成时间等），通过唯一约束确保同一用户对同一知识条目仅有一条记录。

### 6.5 成就系统

- `af_achievement`：成就规则（条件类型/条件值/奖励经验/状态）。
- `af_user_achievement`：用户成就解锁与进度。

### 6.6 运营配置

- `af_carousel`：轮播图配置（图片链接、跳转链接、排序、启用）。
- `af_system_config`：系统配置项（键值对，可扩展为 JSON 配置）。

---

## 7. 典型业务流程（用于答辩演示脚本）

### 7.1 “训练—统计—成就”闭环

1. 用户登录进入训练页；  
2. 系统随机推送一条诈骗案例；  
3. 用户完成判断并提交，后端落库训练记录；  
4. 训练统计接口返回累计训练次数与正确率；  
5. 若达到成就条件（如训练次数阈值），前端成就页可展示解锁进度（可扩展为 WebSocket 推送即时提醒）。  

### 7.2 “测评—报告—风险等级更新”闭环

1. 用户进入测评页获取题目与选项；  
2. 用户提交答卷；  
3. 后端按维度加权并归一化评分，生成风险等级与建议；  
4. 结果写入测评表并同步更新用户表 `risk_level`；  
5. 前端展示报告详情，同时在个人中心展示最新风险画像。  

### 7.3 “知识学习—进度记录—完成统计”闭环

1. 用户浏览知识列表并打开详情；  
2. 前端上报学习进度（达到 100 视为完成）；  
3. 后端写入/更新学习记录；  
4. 前端通过进度统计接口展示完成率与已学列表。  

---

## 8. 说明与扩展建议（面向论文与后续迭代）

- **安全性扩展**：建议引入 JWT、RBAC 角色权限、接口鉴权拦截器，并对上传接口增加更严格的类型校验与大小限制。  
- **测评模型扩展**：可引入更细粒度维度、动态阈值配置或数据驱动的权重校准；保留“可解释输出”作为系统特色。  
- **统计能力扩展**：补充训练/学习/测评多口径统计与可视化，形成“教学效果评估”模块。  
- **可用性扩展**：增加日志、审计与异常追踪，支持稳定演示与问题定位。  

